{
  "name": "texas-will-generator",
  "private": true,
  "workspaces": ["apps/web", "packages/shared"],
  "scripts": {
    "web:dev": "npm --workspace apps/web run dev",
    "web:test": "npm --workspace apps/web run test"
  }
}
